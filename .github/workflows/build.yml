name: Build Binaries

on:
  push:
    tags:
      - 'v*'
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build-linux:
    runs-on: ubuntu-22.04
    outputs:
      artifact: hidden-service-vanity-linux-x86_64-${{ steps.version.outputs.VERSION }}.tar.gz
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Build (release)
        run: cargo build --release --target x86_64-unknown-linux-gnu

      - name: Strip binary
        run: strip target/x86_64-unknown-linux-gnu/release/hidden-service-vanity

      - name: Get version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create archive
        run: |
          cd target/x86_64-unknown-linux-gnu/release
          tar -czvf hidden-service-vanity-linux-x86_64-${{ steps.version.outputs.VERSION }}.tar.gz hidden-service-vanity

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-archive
          path: target/x86_64-unknown-linux-gnu/release/*.tar.gz
          if-no-files-found: error

  build-windows:
    runs-on: windows-2022
    outputs:
      artifact: hidden-service-vanity-windows-x86_64-${{ steps.version.outputs.VERSION }}.zip
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Build (release)
        run: cargo build --release --target x86_64-pc-windows-msvc

      - name: Get version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create archive
        run: |
          cd target/x86_64-pc-windows-msvc/release
          7z a hidden-service-vanity-windows-x86_64-${{ steps.version.outputs.VERSION }}.zip hidden-service-vanity.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-archive
          path: target/x86_64-pc-windows-msvc/release/*.zip
          if-no-files-found: error

  release:
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - name: Download Linux archive
        uses: actions/download-artifact@v4
        with:
          name: linux-archive
          path: ./artifacts

      - name: Download Windows archive
        uses: actions/download-artifact@v4
        with:
          name: windows-archive
          path: ./artifacts

      - name: Generate checksums
        run: |
          cd artifacts
          sha256sum *.tar.gz *.zip > checksums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/*
          generate_release_notes: true
